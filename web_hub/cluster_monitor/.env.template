# 集群监控系统环境配置模板
# ==========================================
# 复制此文件为 .env 并修改实际配置值
# 警告：请勿将包含真实密码的 .env 文件提交到代码仓库
# ==========================================

# ===========================================
# 基本配置
# ===========================================
CHECK_INTERVAL=10
FORUM_MONITORING_ENABLED=true
REQUEST_TIMEOUT=30
MAX_RETRIES=3

# 开发功能（默认禁用，生产静默）
ENABLE_MOCK_DATA=false

# 任务分发策略
# 可选值: least_busy, priority, round_robin
TASK_DISPATCH_STRATEGY=least_busy

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=logs/forum_monitor.log

# 机器配置文件
MACHINES_CONFIG_FILE=machines.txt

#
# 论坛凭证（推荐在项目根目录 .env 配置）
# FORUM_USERNAME=你的用户名
# FORUM_PASSWORD=你的密码
# 也兼容：AICUT_ADMIN_USERNAME / AICUT_ADMIN_PASSWORD


# Web界面配置
WEB_REFRESH_INTERVAL=10

# ===========================================
# 配置说明
# ===========================================
# 注意：推荐在“项目根目录”的 .env 配置（已从根目录启动）
# 论坛 URL、用户名、密码等建议统一放在 根目录 .env
# 如需本目录覆盖，仅在从本目录启动时才会生效
# 论坛地址在 config/forum_settings.yaml 中配置
# ===========================================

# ===========================================
# 安全说明
# ===========================================
# 1. 复制此文件为 .env：cp .env.template .env
# 2. 修改 FORUM_USERNAME 和 FORUM_PASSWORD 为实际值
# 3. 确保密码强度：至少8位，包含字母数字组合
# 4. 不要将 .env 文件提交到代码仓库
# 5. 定期更换密码提高安全性
# ===========================================
