# 🔍 FunClip 集群监控系统专用依赖
# Cluster Monitor Standalone Dependencies
# 
# 职责：确保cluster_monitor目录能够完全独立运行
# 大小：约150MB
# 平台：通用（所有平台共享）

# ==========================================
# 系统基础工具（来自Layer 1）
# ==========================================
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0
psutil>=5.8.0,<8.0.0
python-dotenv>=1.0.0,<2.0.0
python-dateutil>=2.8.0,<3.0.0
chardet>=5.0.0,<6.0.0

# ==========================================
# Web框架和服务器
# ==========================================
flask>=2.3.3,<4.0.0
werkzeug>=2.3.7,<4.0.0
waitress>=2.1.2,<3.0.0

# ==========================================
# 文本处理和HTML解析（来自Layer 3）
# ==========================================
beautifulsoup4>=4.12.0,<5.0.0
lxml>=4.9.0,<5.0.0

# ==========================================
# 数据存储
# ==========================================
redis>=5.0.0,<6.0.0

# ==========================================
# 可选增强功能
# ==========================================
# 更好的JSON处理
# ujson>=5.8.0,<6.0.0

# 更好的日志格式
# colorlog>=6.7.0,<7.0.0

# 定时任务支持
# schedule>=1.2.0,<2.0.0

# ==========================================
# 版本说明
# ==========================================
# flask/werkzeug: Web框架，提供监控API服务
# waitress: 生产级WSGI服务器，替代开发服务器
# requests: HTTP客户端，与工作节点通信
# beautifulsoup4/lxml: HTML解析，论坛内容提取
# redis: 缓存和任务队列，集群状态管理
# psutil: 系统资源监控
# python-dotenv: 环境变量管理

# ==========================================
# 独立部署特性
# ==========================================
# 1. 完全独立：不依赖主系统任何模块
# 2. 轻量级：只包含监控必需的依赖
# 3. 生产就绪：包含生产级服务器
# 4. 容错性：支持Redis可选，SQLite备用
# 5. 可扩展：预留增强功能接口

# ==========================================
# 使用场景
# ==========================================
# 1. 独立部署集群监控系统
# 2. 论坛内容监控和任务分发
# 3. 工作节点状态管理
# 4. 任务队列和负载均衡
# 5. 系统监控和日志记录

# ==========================================
# 设计原则
# ==========================================
# 1. 最小依赖：只包含必需的包
# 2. 版本兼容：与主系统保持一致
# 3. 独立性：确保完全独立运行
# 4. 稳定性：选择稳定的版本范围
