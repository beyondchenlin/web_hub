# web_hub 子项目依赖清单（基于代码遍历）
# Python 3.10+

# ==================== 核心依赖（代码中直接 import） ====================
# 网络请求与解析
requests>=2.31.0,<3.0.0
urllib3>=2.0.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0

# Web 服务
Flask>=2.3.3,<4.0.0
Jinja2>=3.0.0,<4.0.0

# 配置与序列化
python-dotenv>=1.0.0,<2.0.0
PyYAML>=6.0.0,<7.0.0

# 数据存储与队列
redis>=5.0.0,<6.0.0

# 系统监控
psutil>=5.8.0,<8.0.0
GPUtil>=1.4.0,<2.0.0


# ==================== 可选依赖（按需安装） ====================
# 浏览器自动化（aicut_forum_crawler 支持 Selenium 登录）
# selenium>=4.10.0,<5.0.0

# 浏览器自动化（仅 archived/browser_mcp_server.py 使用）
# mcp>=0.9.0,<1.0.0
# playwright>=1.40.0,<2.0.0

# HTML 解析加速（BeautifulSoup 可选解析器）
# lxml>=4.9.0,<5.0.0

# Redis 性能优化（可选）
# hiredis>=2.2.0,<3.0.0

# 生产级 WSGI（可选）
# waitress>=2.1.2,<3.0.0

# GPU 监控增强（当前代码使用 nvidia-smi/GPUtil，可不装）
# pynvml>=11.5.0,<13.0.0

# ==================== 视频处理（pre.pipeline 依赖，task_processor 动态导入） ====================
# 如需启用 pre.* 视频流水线，请安装：
# moviepy==1.0.3
# imageio>=2.31.0,<3.0.0
# imageio-ffmpeg>=0.6.0
# av>=10.0.0,<11.0.0
# auto-editor>=24.0.0,<30.0.0
# Pillow>=10.0.0,<11.0.0
# opencv-python>=4.8.0,<5.0.0

# ==================== 说明 ====================
# 本清单依据对 web_hub 目录下 Python 文件的 import 实际遍历：
# - 直接 import 的三方库：requests, urllib3, beautifulsoup4, Flask, Jinja2,
#   python-dotenv, PyYAML, redis, psutil, GPUtil
# - 可选：selenium（登录辅助）、mcp/playwright（仅 archived 脚本）、lxml、hiredis、waitress、pynvml
# - ASR（语音识别）默认关闭。如需启用，请安装 funasr 及其依赖并在代码中启用相关功能
# - 视频流水线所需的依赖在代码中以 pre.* 动态加载，默认不安装
# - 未在代码中使用的包（如 pandas, rich, tqdm, python-dateutil 等）已移除以保持最小化
